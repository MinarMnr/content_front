import { FormGroup } from "../lib/formGroup";

export const required: Function = (control: string, form: FormGroup, setForm: Function): void => {
    setForm({
        ...form,
        
    })
};

export const minLength: Function = (control: string, form: FormGroup, setForm: Function): void => {
    setForm({
        ...form,
        
    })
};